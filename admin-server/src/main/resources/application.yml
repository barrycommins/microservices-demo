eureka:
  client:
    register-with-eureka: false
server:
  port: 9090
---
spring:
  profiles:
    active: docker
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka